from django.urls import path

from .views import ListCategory,CreateCategory,UpdateCategory,DeleteCategory

urlpatterns=[

    path('',ListCategory.as_view(), name='list_category'),

    path('<pk>/update',UpdateCategory.as_view(), name='update_category'),

    path('<pk>/delete',DeleteCategory.as_view(), name='delete_category'),

    path('create', CreateCategory.as_view(), name='create_category'),

]